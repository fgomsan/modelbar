# First task — Librarian

Send this after creating the Bot. No connectors required if the kit is already on disk.

## Task

> Read `/workspace/grok-bots/AGENTS.md` and `README.md` (adjust the path if the clone lives elsewhere). List every Bot in `bots/`, every skill in `.grok/skills/`, and every routine in `routines/`. For each Bot, quote its Name, Title, and the first standing rule. Return a roster table and a one-paragraph gap list (missing profiles, skills without an owner, routines without a tested skill). Do not create Bots, skills, or routines. Do not push git. Do not change files yet.

## When that looks right

> Save the process we just used as a skill called “Load this kit.” Include the clone path, the roster table format, the rule that secrets never go in git, and that creating Bots or routines always needs my approval.

Also copy any wording fixes into `.grok/skills/load-this-kit/SKILL.md` in this repo.
